<?php

namespace {{namespace}};

use Filter\NodeFilter;
use Illuminate\Database\Eloquent\Builder;

class {{class}} implements NodeFilter
{
    public function handle(Builder $query, $input): Builder
    {
        // return $query->where('id', $input);
    }
}